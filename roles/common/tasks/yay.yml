- name: aur spotify
  become: yes
  become_user: "{{ username }}"
  ignore_errors: yes
  aur:
    use: yay
    name:
      - spotify

    skip_pgp_check:

- name: aur sublime and xmind
  become: yes
  become_user: "{{ username }}"
  ignore_errors: yes
  aur:
    use: yay
    name:
      - sublime-text-4
      - xmind
    skip_pgp_check:

- name: aur rcm
  become: yes
  become_user: "{{ username }}"
  ignore_errors: yes
  aur:
    use: yay
    name:
      - rcm
    skip_pgp_check: